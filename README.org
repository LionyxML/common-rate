# common-rate

A small toy project written in SBCL to fetch the current rate between
two assets.

Use `make` to build it, run `./common-rate --help` to get help.

Based on the works of <https://github.com/parenworks/gilt>

For more `make` options run `make help`.
